# Day 4 执行计划

- 计划日期：`2026-03-26`
- 负责人：`Gemini CLI`
- 状态：`Not Started`
- 对应阶段：`Day 4`

## 今日主目标
- 在 `packages/agent-runtime` 中搭建 **Agent 状态机 (State Machine)** 骨架，实现对话逻辑的解耦。
- 实现基于 `Zod` 的 **结构化输出 (Structured Output)** 校验，确保 LLM 返回的 JSON 格式稳定。
- 定义基础的 **Prompt 模板** 系统，支持 Persona 注入和上下文组装。

## 逐小时清单
- `09:00-10:00` **架构设计**：定义 `AgentState` 类型（idle, thinking, speaking, archiving）和 `AgentEvent` 接口。
- `10:00-11:30` **状态机实现**：编写 `AgentStateMachine` 类，实现状态流转逻辑，暂不接入真实 LLM。
- `11:30-12:30` **结构化 Schema**：定义 `Thought` 和 `Response` 的 Zod Schema（包含行为意图、语气控制、内容、待提取记忆）。
- `13:30-15:00` **Prompt 工程**：在 `packages/agent-runtime` 中实现 `PromptManager`，支持从数据库加载 Persona 并替换模板变量。
- `15:00-17:00` **Mock 联调**：编写单元测试，模拟 LLM 返回结构化 JSON，验证状态机能正确解析并触发“记忆提取”或“消息发送”事件。
- `17:00-18:00` **API 接入准备**：在 `apps/api` 中定义调用 Runtime 的 Service 接口占位。
- `19:00-20:30` **故障处理逻辑**：实现 JSON 解析失败、Token 超限或 LLM 异常时的重试与回退（Fallback）策略。
- `20:30-21:00` **总结与同步**：更新进度文档，提交代码。

## 今日产出物
- `packages/agent-runtime/src/state-machine/`：状态机核心逻辑。
- `packages/agent-runtime/src/schemas/`：结构化输出的 Zod 定义。
- `packages/agent-runtime/src/prompts/`：基础 System Prompt 模板。
- 状态机单元测试集。

## 验收标准
- 状态机能通过单元测试完成 `Input -> Thinking -> Output -> Memory` 的闭环（使用 Mock LLM）。
- 输入非法 JSON 时，系统能触发解析错误并进入错误处理状态。
- Prompt 模板能正确注入 Persona 的 traits 和 bio。

## 结束前需要更新
- 在 `plans/progress-tracker.md` 里更新 Day 4 的实际结果和状态。
- 准备 Day 5：Persona Builder 的实现计划。

## 潜在风险
- **Token 窗口管理**：多轮对话后上下文可能过长，需预留 Token 计数逻辑。
- **结构化输出不稳定性**：部分轻量级模型可能不严格遵循 JSON 模式，需加强正则提取兜底。
